import {  reqGet,reqPost } from "../../utils/request"
const state = {
}

const mutations = {
}

const actions = {
}

const getters = {
    userList(state) {
        return state.userList
    },
}

export default {
    state,
    mutations,
    actions,
    getters,
    namespaced: true
}